@model IEnumerable<string>
@{
    ViewBag.Title = "Videos de interés";
}

@foreach (var url in Model) {

    <div class="embed-responsive embed-responsive-16by9">
        <iframe class="embed-responsive-item" src="@url"></iframe>
    </div>

}